import Link from "next/link";import {KeyRound} from "lucide-react";import {login} from "./actions";export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <main className="loginWrap"><section className="loginCard"><Link href="/" className="brand loginBrand"><span className="brandMark">S</span><span><strong>Savrdh Credit</strong><small>Secure role-based access</small></span></Link><h1>Sign in</h1><p>Access is granted by assigned role. Sensitive finance actions require Finance or Owner authorization.</p><form action={login}><div className="formField"><label htmlFor="email">Work email</label><input id="email" name="email" type="email" autoComplete="email" required/></div><div className="formField"><label htmlFor="password">Password</label><input id="password" name="password" type="password" autoComplete="current-password" required/></div>{error&&<p className="errorText">Login failed. Check your credentials and role access.</p>}<button className="primaryButton" type="submit"><KeyRound size={16}/>Sign in securely</button></form></section></main>}