import {NextResponse} from "next/server";import {ZodError} from "zod";import {verifyDisbursement,verifyDisbursementSchema} from "@/lib/domain/finance";export async function POST(req:Request){try{const body=verifyDisbursementSchema.parse(await req.json());return NextResponse.json({ok:true,data:await verifyDisbursement(body)})}catch(e){return NextResponse.json({ok:false,error:e instanceof ZodError?"Invalid payload":e instanceof Error?e.message:"Verification failed"},{status:e instanceof ZodError?400:403})}}